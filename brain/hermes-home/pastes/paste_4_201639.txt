已按要求执行本次 incremental_build，并完成同步与判定。结果：skipped:no_raw_changes（未触发图谱增量更新，未 commit，未 push，未发送 Telegram）。

日志文件：logs/webhook-runs/gh-25458574250-1.md

- 已执行保护性流程：stash -> fetch/checkout/pull --ff-only -> stash pop
- 差异检测：
  - git diff --name-only 81c142c4f4fc517740312af41beda7ae1ddce191..0e7a12998e4b298214567465049a159faffb9792 -- '*.md'
  - 回退：git diff --name-only HEAD~1..HEAD -- '*.md'
  - 两者均仅命中：logs/webhook-runs/gh-25458462144-1.md
  - raw/**/*.md 过滤后为空

---

1) 本次触发的 raw 文章清单（仅 raw/**/*.md）
无

2) 本次新增实体（name + 文件路径）
无

3) 本次新增关系（source -> relation -> target）
无

4) 本次更新实体（name + 变化类型）
无

5) 构建产物摘要（本次改动了哪些 wiki/entities/wiki/concepts/wiki/index/wiki/log）
- 未执行 wiki 图谱增量改写（按规则停止）
- 仅新增运行日志：
  - logs/webhook-runs/gh-25458574250-1.md

6) commit/push 结果（hash）
- commit：无（skipped:no_raw_changes）
- push：无（skipped:no_raw_changes）

（按你的规则：本次未发送 Telegram）